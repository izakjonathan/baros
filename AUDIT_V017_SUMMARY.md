# v0.17.0 architecture audit summary

- Total release files reviewed: 144
- CSS files: 5
- TS/TSX files: 73
- API route files: 31
- Database migrations retained: 11
- GitHub workflows retained: 2

## CSS ownership

- `app/globals.css`: root import and layer declaration only.
- `app/styles/tokens.css`: semantic values.
- `app/styles/reset.css`: browser/document defaults.
- `app/styles/legacy-geometry.css`: low-priority transitional geometry.
- `app/styles/components.css`: canonical component presentation.

The canonical component file was generated by parsing the previous final-interface section and merging repeated selectors inside the same at-rule scope. The embedded v0.15 repair marker and separate v0.16 safeguard layer are gone.

## Automated validation

- Complete `npm run test:all`: passed.
- CSS parse errors: none.
- Undefined CSS variables: none except Next.js runtime font variables explicitly allowlisted.
- `!important`: none.
- Obsolete `interface-v016.css` and `design-tokens.css`: removed.
- Service-worker cleanup remains enabled.
- Database schema: unchanged.

## Remaining transitional debt

`legacy-geometry.css` remains large and intentionally low priority. It has not been falsely described as eliminated. Future releases should migrate one feature at a time from that file into clearly owned component rules, with browser screenshots and production builds as acceptance gates.
