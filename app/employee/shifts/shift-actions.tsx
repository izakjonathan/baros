"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { ArrowRightLeft, Hand, X } from "lucide-react";
type Person={id:string;name:string}; type SwapShift={id:string;label:string};
export function ClaimShiftButton({shiftId}:{shiftId:string}){const router=useRouter();const [state,setState]=useState<'idle'|'saving'|'done'|'error'>('idle');async function claim(){setState('saving');const r=await fetch('/api/shift-claims',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({shiftId})});if(r.ok){setState('done');router.refresh()}else setState('error')}return <button type="button" className="portal-action primary-action" disabled={state==='saving'||state==='done'} onClick={claim}>{state==='done'?'Request sent':state==='saving'?'Sending…':state==='error'?'Try again':'Request shift'}</button>}
export function TransferResponse({transferId}:{transferId:string}){const router=useRouter();const [busy,setBusy]=useState(false);async function respond(accept:boolean){setBusy(true);const r=await fetch('/api/shift-transfers',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({transferId,accept})});setBusy(false);if(r.ok)router.refresh()}return <div className="transfer-response"><button type="button" disabled={busy} className="secondary" onClick={()=>respond(false)}>Decline</button><button type="button" disabled={busy} className="primary" onClick={()=>respond(true)}>Accept</button></div>}
export function TransferShiftButton({shiftId,people,swapShifts}:{shiftId:string;people:Person[];swapShifts:SwapShift[]}){const router=useRouter();const [open,setOpen]=useState(false),[type,setType]=useState<'HANDOVER'|'SWAP'>('HANDOVER'),[target,setTarget]=useState(people[0]?.id||''),[swap,setSwap]=useState(swapShifts[0]?.id||''),[note,setNote]=useState(''),[state,setState]=useState('idle');async function submit(){setState('saving');const r=await fetch('/api/shift-transfers',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({shiftId,type,targetEmployeeId:target||null,swapShiftId:type==='SWAP'?swap:null,note})});if(r.ok){setState('done');setOpen(false);router.refresh()}else setState('error')}return <>{<button type="button" className="portal-action" disabled={!people.length} onClick={()=>setOpen(true)}><ArrowRightLeft size={14}/>Hand over / swap</button>}{state==='done'&&<small role="status">Request sent</small>}{open&&<div className="modal-layer"><button type="button" className="modal-scrim" onClick={()=>setOpen(false)} aria-label="Close"/><section className="modal" role="dialog" aria-modal="true" aria-labelledby="transfer-title"><div className="modal-head"><div><h2 id="transfer-title">Change this shift</h2><p>The other employee responds first, then a manager approves.</p></div><button type="button" className="icon-button" onClick={()=>setOpen(false)} aria-label="Close"><X size={18}/></button></div><div className="assignment-toggle"><button type="button" className={type==='HANDOVER'?'selected':''} onClick={()=>setType('HANDOVER')}><Hand size={14}/>Hand over</button><button type="button" className={type==='SWAP'?'selected':''} onClick={()=>setType('SWAP')}><ArrowRightLeft size={14}/>Swap</button></div><div className="transfer-dialog"><label>Employee<select value={target} onChange={e=>setTarget(e.target.value)}>{people.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></label>{type==='SWAP'&&<label>Their shift<select value={swap} onChange={e=>setSwap(e.target.value)}>{swapShifts.map(s=><option value={s.id} key={s.id}>{s.label}</option>)}</select></label>}<label>Note<textarea rows={3} value={note} onChange={e=>setNote(e.target.value)}/></label>{state==='error'&&<p className="form-error">Could not send request.</p>}</div><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setOpen(false)}>Cancel</button><button type="button" className="primary" disabled={!target||state==='saving'||(type==='SWAP'&&!swap)} onClick={submit}>{state==='saving'?'Sending…':'Send request'}</button></div></section></div>}</>}
