"use client"; import { useState } from "react"; import { Check } from "lucide-react";
export function RequestForm({type}:{type:'TIME_OFF'|'AVAILABILITY'}){const [from,setFrom]=useState(''),[to,setTo]=useState(''),[note,setNote]=useState(''),[done,setDone]=useState(false),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({type,startsAt:new Date(from).toISOString(),endsAt:new Date(to).toISOString(),note})});setBusy(false);if(r.ok)setDone(true)}if(done)return <div className="request-success"><Check/><h2>Request submitted</h2><p>Your manager will be notified and can review it.</p><button className="secondary" onClick={()=>setDone(false)}>Create another</button></div>;return <form className="portal-form" onSubmit={submit}><label>From<input type="datetime-local" value={from} onChange={e=>setFrom(e.target.value)} required/></label><label>Until<input type="datetime-local" value={to} onChange={e=>setTo(e.target.value)} required/></label><label>Note<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Add any useful context for your manager" rows={4}/></label><button className="primary full" disabled={busy}>{busy?'Submitting…':'Submit request'}</button></form>}
