import { createHash,timingSafeEqual } from "node:crypto";import { NextResponse } from "next/server";import { db } from "@/lib/db/client";import { enforceRateLimit } from "@/lib/security/rate-limit";import { distanceMeters } from "@/lib/security/geofence";
const hash=(v:string)=>createHash('sha256').update(v).digest();
export async function POST(req:Request){const b=await req.json();const ip=req.headers.get('x-forwarded-for')?.split(',')[0]||'unknown';try{await enforceRateLimit(`kiosk:${ip}`,20,60)}catch{return NextResponse.json({error:'Too many attempts'},{status:429})}
 const [employee]=await db()`select e.*,l.latitude,l.longitude,l.clock_radius_meters from employees e join employee_locations el on el.employee_id=e.id join locations l on l.id=el.location_id where l.id=${b.locationId} and e.active and e.kiosk_pin_hash is not null and (e.pin_locked_until is null or e.pin_locked_until<now())`;
 if(!employee)return NextResponse.json({error:'Invalid PIN'},{status:401});const expected=Buffer.from(employee.kiosk_pin_hash,'hex');const actual=hash(String(b.pin||''));if(expected.length!==actual.length||!timingSafeEqual(expected,actual)){await db()`update employees set pin_failed_attempts=pin_failed_attempts+1,pin_locked_until=case when pin_failed_attempts>=4 then now()+interval '15 minutes' else pin_locked_until end where id=${employee.id}`;return NextResponse.json({error:'Invalid PIN'},{status:401})}
 if(employee.latitude!=null&&employee.longitude!=null&&b.latitude!=null&&b.longitude!=null){const d=distanceMeters(Number(employee.latitude),Number(employee.longitude),Number(b.latitude),Number(b.longitude));if(d>Number(employee.clock_radius_meters)){await db()`insert into attendance_alerts(organization_id,location_id,employee_id,alert_type,severity,message) values(${employee.organization_id},${b.locationId},${employee.id},'GEOFENCE','WARNING',${`Clock attempt ${Math.round(d)}m from venue`})`;return NextResponse.json({error:'Outside approved clock-in area',distanceMeters:Math.round(d)},{status:403})}}
 await db()`update employees set pin_failed_attempts=0,pin_locked_until=null where id=${employee.id}`;return NextResponse.json({employeeId:employee.id,name:`${employee.first_name} ${employee.last_name}`,verified:true});}
