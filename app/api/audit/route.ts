import { NextResponse } from "next/server"; import { hasCapability } from "@/lib/auth/capabilities"; import { getSessionUser } from "@/lib/auth/session"; import { db } from "@/lib/db/client";
export async function GET(){const u=await getSessionUser();if(!u||!hasCapability(u.role,"control.read"))return NextResponse.json({error:'Forbidden'},{status:403});return NextResponse.json(await db()`select a.*,actor.name actor_name from audit_logs a left join users actor on actor.id=a.actor_user_id where a.organization_id=${u.organizationId} order by a.created_at desc limit 100`)}
