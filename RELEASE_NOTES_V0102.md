# Bar Ops v0.10.2 — Compact controls and employee pay

- Corrects clipped Week/Month/Custom selector labels.
- Removes the remaining selector background treatment.
- Resizes custom date-range fields for mobile Safari.
- Keeps Add employee aligned with the Team title.
- Compresses Time & attendance filters into a two-column mobile grid.
- Places shift start and end times side by side and makes the date field full width.
- Removes old repeat-panel surface colours.
- Prevents the workspace loading screen from flashing twice during initial location resolution.
- Adds hourly pay rate to employee create/edit and PostgreSQL persistence.
- Removes automatic focus from the employee name field, preventing the keyboard from opening immediately.
