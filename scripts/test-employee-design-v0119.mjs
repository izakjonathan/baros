import fs from "node:fs";
const css=fs.readFileSync("app/design-system.css","utf8");
const shell=fs.readFileSync("app/employee/employee-shell.tsx","utf8");
const layout=fs.readFileSync("app/employee/layout.tsx","utf8");
const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));
for (const value of ["employee-location","employee-header-actions","var(--type-page)","var(--color-canvas)","var(--control-height)","employee-nav"]) if(!css.includes(value)&&!shell.includes(value)) throw new Error(`Missing aligned employee design: ${value}`);
if(!layout.includes("select name from locations")) throw new Error("Employee shell does not use location context");
if(!shell.includes("IconButton")||!shell.includes("status-dot")) throw new Error("Employee header is not using shared design primitives");
if(!["0.11.9","0.12.0","0.12.1"].includes(pkg.version)) throw new Error("Incorrect release version");
console.log("Employee design alignment v0.11.9 checks passed");
