import fs from "node:fs";
const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));
const schedule=fs.readFileSync("features/scheduling/ScheduleWorkspace.module.css","utf8");
const shell=fs.readFileSync("components/shell/ManagerShell.module.css","utf8");
const fail=(message)=>{console.error(`v0.18.4.24 regression: ${message}`);process.exit(1)};
if(pkg.version!=="0.18.4.24") fail("package version mismatch");
if(!schedule.includes("v0.18.4.24 compact Shift Plan header stack")) fail("compact header stack CSS missing");
if(!schedule.includes("font-size:1.72rem !important")) fail("mobile title compaction missing");
if(!schedule.includes("height:1.75rem !important")) fail("mobile week controls are not compact");
if(!schedule.includes("gap:.34rem")) fail("mobile workspace gap reduction missing");
if(!shell.includes("v0.18.4.24 compact mobile top bar")) fail("compact mobile top bar CSS missing");
if(!shell.includes("height:2.9rem")) fail("mobile top-bar height not reduced");
console.log("v0.18.4.24 compact Shift Plan header regression passed");
