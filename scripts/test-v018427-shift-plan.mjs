import fs from "node:fs";
const pkg = JSON.parse(fs.readFileSync("package.json", "utf8"));
const css = fs.readFileSync("features/scheduling/ScheduleWorkspace.module.css", "utf8");
const fail = (message) => { console.error(`v0.18.4.27 regression: ${message}`); process.exit(1); };
if (pkg.version !== "0.18.4.27") fail("package version mismatch");
if (!css.includes("v0.18.4.27 calendar-first hierarchy")) fail("calendar-first CSS missing");
if (!css.includes("background:transparent !important")) fail("background-level header treatment missing");
if (!css.includes("grid-auto-columns:minmax(6.75rem,29vw)")) fail("mobile calendar column sizing missing");
if (!css.includes("scroll-snap-type:x proximity")) fail("calendar scroll behavior missing");
if (!css.includes("background:#dfee4b !important")) fail("neon day header treatment missing");
if (!css.includes("border:1px dashed rgba(255,244,196,.6)")) fail("per-day add-shift affordance missing");
console.log("v0.18.4.27 calendar-first shift-plan regression passed");
