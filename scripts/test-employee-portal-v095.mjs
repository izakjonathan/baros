import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const shell=read('app/employee/employee-shell.tsx'); const shifts=read('app/employee/shifts/page.tsx'); const clock=read('app/api/time-clock/route.ts'); const avail=read('app/employee/availability/availability-editor.tsx');
const checks=[['compact More navigation',shell.includes('employee-more-sheet')&&shell.includes('aria-current')],['published-only employee shifts',shifts.includes("s.status in ('PUBLISHED','CONFIRMED')")&&shifts.includes("s.status='PUBLISHED'")],['incoming transfer response',shifts.includes('Needs your response')&&shifts.includes('TransferResponse')],['parallel schedule queries',shifts.includes('Promise.all')],['geofence enforcement',clock.includes('require_location_check')&&clock.includes('clock_radius_meters')],['early clock-in settings',clock.includes('early_clock_in_minutes')],['recurring availability editor',avail.includes("fetch('/api/availability'")&&avail.includes("method:'PUT'")],['loading and error boundaries',fs.existsSync('app/employee/loading.tsx')&&fs.existsSync('app/employee/error.tsx')],['notification centre',fs.existsSync('app/employee/notifications/page.tsx')]];
let failed=false;for(const [name,ok] of checks){console.log(`${ok?'✓':'✗'} ${name}`);if(!ok)failed=true}if(failed)process.exit(1);
