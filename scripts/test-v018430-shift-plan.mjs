import fs from "node:fs";
const pkg = JSON.parse(fs.readFileSync("package.json", "utf8"));
const css = fs.readFileSync("features/scheduling/ScheduleWorkspace.module.css", "utf8");
const fail = (message) => { console.error(`v0.18.4.30 regression: ${message}`); process.exit(1); };
if (pkg.version !== "0.18.4.30") fail("package version mismatch");
if (!css.includes("v0.18.4.30 divider and day-column density cleanup")) fail("focused CSS block missing");
if (!css.includes("--iphone-day-gap:.2rem")) fail("tighter horizontal day gap missing");
if (!css.includes("min-height:1.72rem")) fail("short day header missing");
if (!css.includes("gap:.22rem !important")) fail("compact day-stack gap missing");
if (!css.includes(".legend{\n  display:none !important")) fail("legend removal missing");
if (!css.includes("border-block:0 !important")) fail("divider removal missing");
console.log("v0.18.4.30 Shift Plan divider and density regression passed");
