import fs from "node:fs";

const read = (file) => fs.readFileSync(file, "utf8");
const operation = read("features/operation/OperationModule.tsx");
const route = read("app/api/operation-module/route.ts");
const imageRoute = read("app/api/operation-images/route.ts");
const imageDeliveryRoute = read("app/api/operation-images/[...pathname]/route.ts");
const operationContent = read("features/operation/content.ts");
const operationStyles = read("features/operation/OperationModule.module.css");
const types = read("features/operation/types.ts");
const migration = read("db/migrations/018_operation_integrity_cleanup.sql");
const taskAudienceMigration = read("db/migrations/019_operation_task_assignment_scope.sql");
const remindersMigration = read("db/migrations/021_operation_reminders.sql");
const cashCountsMigration = read("db/migrations/020_operation_cash_counts.sql");
const publicOperationRoute = read("app/api/operation-public/[accessToken]/route.ts");
const publicOperationLayout = read("app/operation/public/[accessToken]/layout.tsx");
const publicOperationManifest = read("app/operation/public/[accessToken]/manifest.webmanifest/route.ts");
const operationLayout = read("app/operation/layout.tsx");

const checks = [
  ["selected task date is persisted", operation.includes("date: selectedTaskDate, completed: !task.completed")],
  ["failed task/checklist mutations refresh authoritative data", operation.includes('"Could not update task."') && operation.includes('"Could not update checklist."')],
  ["task managers use the shared capability model", route.includes('hasCapability(role, "operations.manage")') && route.includes("requireTaskManager(user.role)")],
  ["Copenhagen service date is explicit", route.includes("operationDateNow()") && read("features/operation/date.ts").includes("Europe/Copenhagen")],
  ["month-end recurrence is handled", types.includes("Math.min(start.getUTCDate(), lastDay)")],
  ["unused governance/reminder/audit schema is removed", ["operation_task_reminders", "operation_article_acknowledgements", "operation_article_versions", "operation_need_events"].every(name => migration.includes(`drop table if exists ${name}`))],
  ["SVG upload is rejected", route.includes("requireTaskManager") && imageRoute.includes("avif|gif|jpe?g|png|webp")],
  ["private image uploads have an authenticated delivery route", imageRoute.includes('access: "private"') && imageRoute.includes("logServerError") && imageDeliveryRoute.includes('access: "private"') && imageDeliveryRoute.includes("getSessionUser")],
  ["uploaded private image paths persist in article content", operationContent.includes("operationImagePath") && operationContent.includes("isArticleImageSource(src)")],
  ["device images use compact previews and readable detail images", operation.includes("async function prepareOperationImages") && operation.includes("previewImageMaxDimension = 960") && operation.includes("photoImageMaxDimension = 1600") && operation.includes("screenshotImageMaxDimension = 2048") && operation.includes('canvas.toBlob(resolve, "image/webp", quality)') && imageRoute.includes('form.get("preview")') && imageRoute.includes('form.get("detail")')],
  ["reader images open their detailed version on demand", operation.includes("function ArticleImage") && operation.includes("fullSrc") && operation.includes("className={styles.imageViewer}") && operationStyles.includes(".readerImageButton{") && operationStyles.includes(".imageViewer{")],
  ["Home presents one clear next action before secondary content", operation.includes("function HomeView") && operation.includes("const nextTask = outstanding[0]") && operation.includes('className={styles.nextAction}') && operation.includes('className={styles.homeShortcuts}') && !operation.includes('SectionHeader title="Tools"') && operationStyles.includes(".nextAction{") && operationStyles.includes(".homeShortcuts{")],
  ["Today news exposes structured readable updates in the page flow", operation.includes("function NewsFeed") && operation.includes("showBody") && operation.includes("function NewsCardBody") && operation.includes("function newsCardBlocksFromTiptapNode") && operation.includes('className={styles.newsCardBodyButton}') && !operation.includes("news.slice(0, 3)") && operationStyles.includes(".newsCardHeader{") && operationStyles.includes(".newsCardBodyButton{") && operationStyles.includes(".newsCardHeading{") && operationStyles.includes(".newsCardSubheading{") && operationStyles.includes(".newsCardBody,.newsCardList{")],
  ["Today news uses a compact mobile reading measure and rhythm", operationStyles.includes("max-width:44ch") && operationStyles.includes("font-size:1rem;font-weight:400;line-height:1.5") && operationStyles.includes("font-size:1.1875rem;font-weight:700;line-height:1.22") && operationStyles.includes("font-size:1.0625rem;font-weight:600;line-height:1.3")],
  ["Today news bullets retain a hanging indent", operation.includes('className={styles.newsCardListMarker}') && operationStyles.includes("grid-template-columns:1rem minmax(0,1fr)")],
  ["Legacy article lists are grouped into one readable list", operation.includes("function RichTextArticle") && operation.includes("while (index < lines.length && lines[index].attributes?.list === list)") && operationStyles.includes(".readerArticle ul,.readerArticle ol{display:grid;gap:.6rem")],
  ["article links can be inserted and followed with reader back navigation", operation.includes('operation://article/${articleId}') && operation.includes('current.state.doc.textBetween') && operation.includes('className={styles.articleLinkPicker}') && operation.includes('className={styles.readerBackCircle}') && operation.includes('openArticle={(article) => setReaderStack(stack => [...stack, article])}') && operationContent.includes('operationArticleHref') && operationContent.includes('operationArticleHref.test(href)') && operationStyles.includes('.articleLinkPicker{') && operationStyles.includes('.readerBackCircle{')],
  ["owners retain News edit, delete and save feedback", operation.includes('canManageContent={state.canManageContent}') && operation.includes('onEdit={canManageContent && editArticle') && operation.includes('Could not save article. Check your connection and try again.') && operation.includes('void refresh(selectedTaskDate).catch(() => undefined)')],
  ["shared staff links install back to their own public workspace", publicOperationLayout.includes("manifest: `${publicPath}/manifest.webmanifest`") && publicOperationManifest.includes("start_url: publicPath") && publicOperationManifest.includes("scope: publicPath") && publicOperationManifest.includes("getPublicOperationAccess(accessToken)") && publicOperationManifest.includes('"cache-control": "private, no-store, max-age=0"')],
  ["owner article management reuses Handbook cards", !operation.includes("function AdminPanel") && operation.includes("canManageContent={state.canManageContent}") && operation.includes("onEdit={canManageContent ? () => editArticle(article) : undefined}")],
  ["article editor header is compact and supports existing or new categories", operation.includes("categories={editorCategories}") && operation.includes('className={styles.editorTopSelect}') && operation.includes('Add new category…') && operationStyles.includes("grid-template-columns:2.25rem minmax(0,1fr) minmax(0,1fr) 2.25rem")],
  ["format controls have an equal-width visible dock", operationStyles.includes("grid-template-columns:repeat(8,minmax(0,1fr))") && operationStyles.includes("border-radius:999px;background:color-mix(in srgb,var(--op-canvas) 96%,transparent)")],
  ["article editor metadata does not inherit full-size form controls", operationStyles.includes(".editorTitleInput{box-sizing:border-box;display:block;min-height:1.55rem;margin:0;padding:0") && operationStyles.includes("font-size:var(--op-type-card)") && operationStyles.includes(".editorDescriptionInput{box-sizing:border-box;display:block;min-height:1.4rem;margin:.1rem 0 .28rem;padding:0") && operationStyles.includes("font-size:var(--op-type-body)")],
  ["format controls sit close to the visual viewport edge", operationStyles.includes("var(--editor-vv-height,100dvh) - 2.95rem")],
  ["format controls expose compact style and numbered-list buttons", operation.includes('aria-label="Text style"') && operation.includes('aria-label="Numbered list"')],
  ["format popovers close on outside interaction", operation.includes('document.addEventListener("pointerdown", closePopovers)')],
  ["article images are borderless", operationStyles.includes(".tiptapSurface :global(.tiptap img){display:block;max-width:100%;height:auto;margin:.85rem 0;border:0")],
  ["task date and time retain a two-column mobile layout", !operationStyles.includes(".moduleGrid,.adminGrid,.editorMeta,.taskSettings{grid-template-columns:1fr}" )],
  ["task instructions are genuinely optional", route.includes('optionalString(body, "description", 300) || ""')],
  ["task audiences are explicit and persisted", taskAudienceMigration.includes("assignment_scope") && route.includes('"ON_SHIFT", "EVERYONE"')],
  ["Reminders separates ordering from practical issues", operation.includes('title="To order"') && operation.includes('title="Issues"') && operation.includes('value="RESTOCK"') && operation.includes('value="NEW_ITEM"') && operation.includes('value="ISSUE"') && operation.includes('value="OUT_OF"')],
  ["Reminder actions match the reminder type", operation.includes('issueList ? "RESOLVED" : "ORDERED"') && operation.includes('"DISMISSED"') && route.includes('["RESOLVED", "DISMISSED"]') && route.includes('["ORDERED", "DISMISSED"]')],
  ["Reminders persist their simple classification", remindersMigration.includes("reminder_type") && remindersMigration.includes("stock_level") && remindersMigration.includes("'RESOLVED','DISMISSED'" )],
  ["Reminder labels remain compact and dropdowns use Operation focus styling", operation.includes('className={styles.reminderStatus}') && operationStyles.includes("white-space:nowrap") && operationStyles.includes(".taskSettings select{-webkit-appearance:none")],
  ["Reminder history preserves the action and date for every audience", operation.includes("History ({history.length})") && operation.includes("actionLabel(need)") && operation.includes("actionDate(need)") && route.includes("n.updated_at") && read("lib/operation-public-access.ts").includes("updatedAt: String(need.updated_at || need.created_at)" )],
  ["Count stores multiple named operational cash entries", cashCountsMigration.includes("create table if not exists operation_cash_counts") && cashCountsMigration.includes("till_amount is not null or change_box_amount is not null") && cashCountsMigration.includes("operational_date") && types.includes("export type OperationCashCount") && operation.includes("function CashCountView")],
  ["Count rolls the bar day back until 04:00 Copenhagen time", read("features/operation/date.ts").includes("operationCountDateNow") && read("features/operation/date.ts").includes("hour < 4") && route.includes("operationCountDateNow()")],
  ["Count accepts either till or change box across staff access", route.includes('entity === "cashCount"') && route.includes("Enter a till amount, a change-box amount, or both.") && publicOperationRoute.includes('entity === "cashCount"') && read("lib/operation-public-access.ts").includes("operation_cash_counts")],
  ["Count presents exact date and time plus optional money fields", operation.includes("formatCountDate") && operation.includes("formatCountAmount") && operation.includes("Change box (kr)") && operationStyles.includes(".countComposer{") && operationStyles.includes(".countRow{")],
  ["Phone navigation exposes persistent icon-and-label destinations", operation.includes('className={styles.operationDock}') && operation.includes('aria-current={view === item ? "page" : undefined}') && operation.includes("item === \"home\" ? House") && operationStyles.includes("bottom:max(.2rem,calc(env(safe-area-inset-bottom) - .75rem))") && operationStyles.includes("flex-direction:column")],
  ["Phone navigation is a low-positioned capsule", operationStyles.includes("border-radius:999px;background:var(--op-ink)") && operationStyles.includes(".operationDock button{") && operationStyles.includes("border:0;border-radius:999px")],
  ["Operation dock uses reversed canvas and ink colors", operationStyles.includes(".operationDock{") && operationStyles.includes("background:var(--op-ink);color:var(--op-canvas)") && operationStyles.includes("min-height:2.75rem")],
  ["Operation navigation adapts to an iPad rail", operationStyles.includes("@media(min-width:48rem)") && operationStyles.includes("left:max(.72rem,env(safe-area-inset-left))") && operationStyles.includes("transform:translateY(-50%)")],
  ["Phone navigation yields to readers, editors and the software keyboard", operation.includes("!currentArticle && !editorOpen") && operationStyles.includes(".operationShell:has(input:focus) .operationDock") && operationStyles.includes(".operationShell:has(textarea:focus) .operationDock{display:none}")],
  ["staff pages do not reserve the removed top navigation", operation.includes("showOperationHeader") && operation.includes("styles.operationContentTop") && operation.includes("showOperationHeader && <header") && operationStyles.includes(".operationContentTop{padding-top:calc(.85rem + env(safe-area-inset-top))}")],
  ["Initial Operations data includes task checklists and reminder history", read("app/operation/page.tsx").includes("t.checklist,") && read("app/operation/page.tsx").includes("checklist_completed") && read("app/operation/page.tsx").includes('checklist: Array.isArray(task.checklist)') && !read("app/operation/page.tsx").includes("and status='NEEDED'")],
  ["Phone reminder cards preserve title width with accessible compact actions", operation.includes('aria-label={`Mark ${need.title}') && operationStyles.includes("@media(max-width:30rem)") && operationStyles.includes(".reminderActions button span{position:absolute")],
  ["task audience is composed beside repeat", operation.includes('<Repeat2 size={15} />Repeat') && operation.includes('<label className={styles.taskAudienceField}><span className={styles.taskSettingLabel}><UserRound size={15} />Task audience')],
  ["task settings constrain and center native date and time controls", operationStyles.includes("contain:inline-size") && operationStyles.includes("max-inline-size:100%") && operationStyles.includes("::-webkit-date-and-time-value") && operationStyles.includes("justify-content:center")],
  ["task checkboxes retain the Operation canvas when unchecked", operationStyles.includes(".taskRow input,.taskChecklist input{box-sizing:border-box") && operationStyles.includes(".taskRow input:checked,.taskChecklist input:checked{background:var(--op-ink)}")],
  ["Operations keeps iPad touch inputs and the editor viewport safe", operation.includes("window.visualViewport") && operation.includes('visualViewport?.addEventListener("resize", syncViewport)') && operationStyles.includes("@media (hover:none) and (pointer:coarse)") && operationStyles.includes("font-size:16px")],
  ["Operation viewport prevents gesture and input zoom across authenticated and shared routes", operationLayout.includes("maximumScale: 1") && operationLayout.includes("userScalable: false") && operationStyles.includes(".operationShell input,.operationShell select,.operationShell textarea{font-size:16px}")],
  ["Handbook New is derived from its immutable creation time", types.includes("createdAt: string") && operationContent.includes("createdAt: String(row.created_at") && operation.includes('const handbookCategories = ["All", "New"') && operation.includes("isNewHandbookArticle(article)") && route.includes("a.created_at,a.updated_at") && read("app/operation/page.tsx").includes("published,created_at,updated_at") && read("lib/operation-public-access.ts").includes("published,created_at,updated_at")],
  ["New handbook cards keep their original category pill and use a small star", operation.includes("function ArticleCategoryPill") && operation.includes("articleCategoryNew") && operationStyles.includes(".articleCategoryNew{")],
  ["Handbook reader close uses the saved Operation canvas fill", operation.includes("styles.handbookCloseCircle") && operationStyles.includes(".handbookCloseCircle{background:var(--op-canvas)!important}")],
  ["Operation typography uses shared reading, metadata and label roles", operationStyles.includes("--op-type-reading:1rem") && operationStyles.includes("--op-type-meta:.8125rem") && operationStyles.includes("--op-type-label:.75rem") && operationStyles.includes("font-size:var(--op-type-reading);line-height:var(--op-leading-reading)")],
  ["Reader hierarchy reserves space after titles and separates list points", operationStyles.includes(".readerArticle h1{margin:0 0 1rem") && operationStyles.includes(".readerArticle h2{margin:1.1rem 0 .48rem") && operationStyles.includes(".readerArticle ul,.readerArticle ol{display:grid;gap:.6rem")],
  ["Operation cards protect readable long titles", operationStyles.includes(".needRow>div,.taskRowTitle{min-width:0}") && operationStyles.includes("overflow-wrap:anywhere;text-wrap:pretty")],
  ["UI Studio prevents unreadable canvas and ink pairs", operation.includes("uiColorContrastRatio") && read("app/api/settings/ui-theme/route.ts").includes("hasReadableUiThemeContrast") && read("lib/ui-theme-shared.ts").includes("export function uiColorContrastRatio")],
  ["owner settings keeps UI Studio and resets histories independently", operation.includes("function OperationSettings") && operation.includes('aria-label="Open Operation settings"') && operation.includes('confirmReset("cashCountHistory")') && operation.includes('confirmReset("reminderHistory")') && operation.includes("Active reminders stay in place") && route.includes('"cashCountHistory", "reminderHistory"') && route.includes("status in ('ORDERED','RESOLVED','DISMISSED')") && route.includes("OPERATION_HISTORY_RESET")],
  ["Operations uses only saved Canvas and Ink derivatives", !/#(?:[0-9a-f]{3,8})\b|rgba\(/i.test(operationStyles) && operationStyles.includes("--op-canvas") && operationStyles.includes("--op-ink") && operationStyles.includes("--op-accent:color-mix") && operationStyles.includes(".taskRow,.needRow{display:grid") && operationStyles.includes("border-radius:.68rem;background:var(--op-canvas)}") && operationStyles.includes(".uiStudioPanel{") && operationStyles.includes(".articleEditorFrame{")],
  ["Operation canvas owns the document and Safari browser background", operation.includes("root.style.backgroundColor = uiTheme.canvasColor") && operation.includes("body.style.backgroundColor = uiTheme.canvasColor") && operation.includes("themeColor.content = uiTheme.canvasColor") && operation.includes("previousRootBackground") && operation.includes("previousThemeColor")],
];

for (const [name, ok] of checks) {
  if (!ok) throw new Error(`Operation integrity check failed: ${name}`);
  console.log(`PASS ${name}`);
}
