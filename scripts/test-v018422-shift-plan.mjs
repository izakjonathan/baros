import fs from "node:fs";
const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));
const css=fs.readFileSync("features/scheduling/ScheduleWorkspace.module.css","utf8");
const modal=fs.readFileSync("app/mono-components.css","utf8");
const fail=(message)=>{console.error(`v0.18.4.22 regression: ${message}`);process.exit(1)};
if(pkg.version!=="0.18.4.22") fail("package version mismatch");
if(!css.includes("v0.18.4.22 schedule column and modal-adjacent grid polish")) fail("schedule polish block missing");
if(!css.includes("background:#000 !important")) fail("black schedule canvas missing");
if(!css.includes("width:100% !important")) fail("column-constrained shift width missing");
if(!css.includes("--schedule-column-gap:.52rem")) fail("independent column gap missing");
if(!modal.includes("v0.18.4.22 compact internally scrolling shift editor")) fail("shift editor polish block missing");
if(!modal.includes("overflow-y:auto")) fail("internal modal scrolling missing");
if(!modal.includes("grid-template-columns:minmax(0,.72fr) minmax(0,1.45fr)")) fail("balanced desktop action layout missing");
console.log("v0.18.4.22 schedule column and shift modal regression passed");
